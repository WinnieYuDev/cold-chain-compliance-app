# Convex: set by `npx convex dev` when you choose cloud (or copy from Convex dashboard)
CONVEX_DEPLOYMENT=
NEXT_PUBLIC_CONVEX_URL=

# Optional: set OPENAI_API_KEY in Convex dashboard (Settings → Environment Variables) for AI features
